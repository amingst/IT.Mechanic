<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IT.Mechanic.Installer.MainPage"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:Icons="clr-namespace:IT.Mechanic.Installer.Components.FontIcons"
             xmlns:components="clr-namespace:IT.Mechanic.Installer.Components.FontIcons"
             Title="Mechanic">
    <VerticalStackLayout Spacing="30" Margin="50">

        <ListView
            x:Name="ProfilesListView"
            ItemsSource="{Binding Profiles}"
            SeparatorColor="Black"
            SeparatorVisibility="Default">
            <ListView.Header>
                <Label
                    Text="Recent Profiles:"
                    FontSize="20"
                    FontAttributes="Bold"
                    Padding="10,5"
                    TextColor="White"
                    HorizontalOptions="FillAndExpand"
                    />
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame BackgroundColor="LightSlateGray" Padding="10" Margin="5" HasShadow="False">
                            <Grid ColumnDefinitions="*,Auto,Auto">
                                <Label Text="{Binding DNS.DomainName}" VerticalOptions="Center" Grid.Column="0" />

                                <components:IconButton
                                    IconFont="FaSolid"
                                    IconPath="{x:Static components:FontAwesome.Play}"
                                    IconSize="20"
                                    IconColor="MediumSpringGreen"
                                    Tooltip="Start"
                                    VerticalOptions="Center"
                                    HorizontalOptions="End"
                                    Grid.Column="1"
                                    Margin="0,0,10,0"
                                    />
                                <components:IconButton
                                    IconColor="White"
                                    Tooltip="ProfileDetails"
                                    IconSize="20"
                                    IconPath="{x:Static components:FontAwesome.Cogs}"
                                    IconFont="FaSolid"
                                    VerticalOptions="Center"
                                    HorizontalOptions="End"
                                    Grid.Column="2"
                                    />
                            </Grid>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button
            Text="Create Profile"
            HorizontalOptions="Center"
            VerticalOptions="End"
            TextColor="MintCream"
            BackgroundColor="LightSlateGray"
            Clicked="OnStartClicked"/>
    </VerticalStackLayout>
</ContentPage>
